#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x;
    int y;
    scanf("%i %i %i", &x, &y);
    int y;
    if(x>y)
        printf("i", x);
    else if(x<y)
        printf("i", y);
    else printf("i", x);
    return 0;
}
